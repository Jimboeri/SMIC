LOCAL nCNT := 0
OPENPRN()
STITLE = 'TEST RUN'
PRNTITLE(STITLE, 'N')
FOR nCNT = 1 TO 70
  ? 'LINE ' + STR(PLINE)
  PLINE++
NEXT
PRNTITLE(STITLE, 'N')
FOR nCNT = 1 TO 70
  ? 'LINE ' + STR(PLINE)
  PLINE++
NEXT
